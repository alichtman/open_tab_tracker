## Build and Install

```bash
$ cd project_root
$ hatch build .
$ pip3 install .
```

```bash
$ poetry publish --build
```

## Bumping Version

We are managing versioning with `hatc

## Bumping Version

We are managing versioning with `hatch`.

```
$ hatch version [ major / minor / patch / b (bump) ]
$ hatch version release
```
